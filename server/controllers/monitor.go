package controllers

import (
	models "site-checker/server/models"
)

// CreateMonitor - create new monitor for the user
func CreateMonitor(userID uint) *models.Monitor {
	monitor := &models.Monitor{}

	return monitor
}
